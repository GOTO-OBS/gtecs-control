############################################################
# File locations
FILE_PATH = string(default=path_not_set)
CONFIG_PATH = string(default=path_not_set)
IMAGE_PATH = string(default=path_not_set)

# General parameters
LOCAL_HOST = string(default='127.0.0.1')

# Common file strings
ORG_NAME = string(default=Gravitational-wave Optical Transient Observer)
TELESCOPE_NAME = string(default=GOTO_sim)
TELESCOPE_NUMBER = integer(default=1)
ROBOTIC_OBSERVER = string(default=GOTO-pilot)

# People to email in emergency
EMAIL_LIST = string_list(default=list('martin.dyer@sheffield.ac.uk'))

# Email account to send emails from
EMAIL_ADDRESS = string(default=goto-observatory@gmail.com)
EMAIL_SERVER = string(default=smtp.gmail.com:587)

# Site location
SITE_NAME = string(default='La Palma')
SITE_LATITUDE = float(default=28.7598742)
SITE_LONGITUDE = float(default=-17.8793802)
SITE_ALTITUDE = float(default=2327)

# use colour and fancy formatting in output?
FANCY_OUTPUT = integer(default=1)

########################################################################
# Daemon parameters
PYRO_TIMEOUT = float(default=5)
DAEMON_CHECK_PERIOD = float(default=1)
DAEMON_SLEEP_TIME = float(default=0.1)

FILE_LOGGING = integer(default=1)
STDOUT_LOGGING = integer(default=1)
REDIRECT_STDOUT = integer(default=1)

DASHBOARD_IP = string(default='127.0.0.1')

########################################################################
# Conditions parameters
MAX_CONDITIONS_AGE = float(default=240)

WEATHER_TIMEOUT = float(default=700.0)
WEATHER_STATIC = float(default=1200.0)
WEATHER_INTERVAL = float(default=10.0)

EXTERNAL_WEATHER_SOURCES = string_list(default=list('goto', 'w1m'))
INTERNAL_WEATHER_SOURCES = string_list(default=list('goto'))
USE_W1M_RAINBOARDS = integer(default=0)

CONDITIONS_JSON_LOCATION = string(default='0.0.0.0')
ROOMALERT_IP = string(default='0.0.0.0')
RAINDAEMON_NAME = string(default='rain_daemon')
RAINDAEMON_IP = string(default='0.0.0.0')
RAINDAEMON_PORT = string(default='9000')

RAIN_BADDELAY = float(default=0.0)
RAIN_GOODDELAY = float(default=300.0)

MAX_HUMIDITY = float(default=75.0)
MAX_INTERNAL_HUMIDITY = float(default=75.0)
CRITICAL_INTERNAL_HUMIDITY = float(default=80.0)
HUMIDITY_BADDELAY = float(default=90.0)
HUMIDITY_GOODDELAY = float(default=300.0)

MAX_WINDSPEED = float(default=35.0)
WINDSPEED_BADDELAY = float(default=90.0)
WINDSPEED_GOODDELAY = float(default=600.0)

WINDGUST_PERIOD = float(default=300.0)
MAX_WINDGUST = float(default=40.0)
WINDGUST_BADDELAY = float(default=90.0)
WINDGUST_GOODDELAY = float(default=600.0)

MIN_DEWPOINT = float(default=5.0)
DEWPOINT_BADDELAY = float(default=90.0)
DEWPOINT_GOODDELAY = float(default=300.0)

MAX_TEMPERATURE = float(default=30.0)
MIN_TEMPERATURE = float(default=1.0)
MAX_INTERNAL_TEMPERATURE = float(default=30.0)
MIN_INTERNAL_TEMPERATURE = float(default=1.0)
CRITICAL_INTERNAL_TEMPERATURE = float(default=0.0)
TEMPERATURE_BADDELAY = float(default=90.0)
TEMPERATURE_GOODDELAY = float(default=300.0)
ICE_BADDELAY = float(default=3600.0)
ICE_GOODDELAY = float(default=86400.0)

INTERNAL_BADDELAY = float(default=60.0)
INTERNAL_GOODDELAY = float(default=600.0)

SUN_ELEVATION_LIMIT = float(default=-10.0)

MIN_UPSBATTERY = float(default=99.9)
UPS_BADDELAY = float(default=60.0)
UPS_GOODDELAY = float(default=10.0)

LINK_URLS = string_list(default=list('google.com', 'sheffield.ac.uk'))
LINK_BADDELAY = float(default=600.0)
LINK_GOODDELAY = float(default=30.0)

IGNORE_HATCH = integer(default=0)
HATCH_BADDELAY = float(default=1800.0)
HATCH_GOODDELAY = float(default=0.0)

MIN_DISKSPACE = float(default=5.0)

MAX_SATCLOUDS = float(default=40.0)
SATCLOUDS_BADDELAY = float(default=1200.0)
SATCLOUDS_GOODDELAY = float(default=1200.0)

DUSTLEVEL_TIMEOUT = float(default=10800.0)
MAX_DUSTLEVEL = float(default=100.0)
DUSTLEVEL_BADDELAY = float(default=21600.0)
DUSTLEVEL_GOODDELAY = float(default=43200.0)

SEEING_TIMEOUT = float(default=3600.0)

########################################################################
# Mount parameters
MIN_ELEVATION = float(default=20.0)
SITECH_HOST = string(default='127.0.0.1')
SITECH_PORT = integer(default=8079)
SITECH_DEBUG = integer(default=0)
FAKE_MOUNT = integer(default=0)

########################################################################
# Interface parameters
FAKE_FLI = integer(default=0)
FAKE_ASA = integer(default=0)

########################################################################
# Filter wheel parameters
FILTER_LIST = string_list(default=list('L', 'R', 'G', 'B', 'C'))

########################################################################
# Focuser parameters
FOCUS_SLACK_REPORTS = integer(default=0)
FOCUS_COMPENSATION_TEST = integer(default=0)
FOCUS_COMPENSATION_VERBOSE = integer(default=0)

########################################################################
# Camera parameters
FRAMETYPE_LIST = string_list(default=list('normal', 'dark'))
CCD_TEMP = float(default=-25.0)
COMPRESS_IMAGES = integer(default=0)

########################################################################
# Power parameters
POWER_CHECK_PERIOD = integer(default=30)
DASHBOARD_ALLOWED_OUTLETS = string_list(default=list('p1', 'p2'))

########################################################################
# Dome parameters
DOME_CHECK_PERIOD = integer(default=5)
DOME_LOCATION = string(default=/dev/ttyS0)
ARDUINO_LOCATION = string(default=dome)
DOME_HEARTBEAT_LOCATION = string(default=/dev/ttyUSB0)
DOME_HEARTBEAT_PERIOD = integer(default=5)
DOME_DEBUG = integer(default=0)
FAKE_DOME = integer(default=0)
QUICK_CLOSE_BUTTON = integer(default=0)
QUICK_CLOSE_BUTTON_PORT = string(default=/dev/ttyUSB0)
DOME_ALARM_DURATION = integer(default=3)
DEHUMIDIFIER_IP = string(default='0.0.0.0')
DEHUMIDIFIER_PORT = integer(default=17494)

########################################################################
# Scheduler parameters
WEIGHTING_WEIGHT = float(default=1)
AIRMASS_WEIGHT = float(default=1)
TTS_WEIGHT = float(default=1)
HARD_ALT_LIM = float(default=10)
HARD_HA_LIM = float(default=8)
BRIGHT_MOON = float(default=1)
GREY_MOON = float(default=0.65)
DARK_MOON = float(default=0.25)
DARK_MOON_ALT_LIMIT = float(default=-5.0)

############################################################
# Sentinel parameters
LOCAL_IVO = string(default='ivo://org.goto-observatory/goto')
VOSERVER_HOST = string(default='68.169.57.253')
VOSERVER_PORT = integer(default=8099)
SENTINEL_SEND_MESSAGES = integer(default=1)

############################################################
# Obs script parameters
FLATS_SKYMEANTARGET_EVEN = float(default=20000)
FLATS_SKYMEANTARGET_ODD = float(default=38000)
FLATS_NUM = integer(default=3)
FLATS_MAXEXPTIME = float(default=300)
FLATS_STEPSIZE = float(default=600)

IERS_A_URL = string(default='http://maia.usno.navy.mil/ser7/finals2000A.all')
IERS_A_URL_BACKUP = string(default='ftp://cddis.gsfc.nasa.gov/pub/products/iers/finals2000A.all')

########################################################################
# Pilot parameters
NUM_DARKS = integer(default=9)
BAD_CONDITIONS_TASKS_PERIOD = integer(default=3600)

########################################################################
# Slack bot parameters
ENABLE_SLACK = integer(default=0)
SLACK_BOT_TOKEN = string(default=slack_token)
SLACK_DEFAULT_CHANNEL = string(default=slack_channel)

###########################################################
[DAEMONS]
    [[mnt]]
        HOST = string(default=localhost)
        PORT = integer(default=9001)
        PROCESS = string(default=mnt_daemon.py)
        PINGLIFE = float(default=10.0)
    [[filt]]
        HOST = string(default=localhost)
        PORT = integer(default=9002)
        PROCESS = string(default=filt_daemon.py)
        PINGLIFE = float(default=10.0)
    [[foc]]
        HOST = string(default=localhost)
        PORT = integer(default=9003)
        PROCESS = string(default=foc_daemon.py)
        PINGLIFE = float(default=10.0)
    [[cam]]
        HOST = string(default=localhost)
        PORT = integer(default=9004)
        PROCESS = string(default=cam_daemon.py)
        PINGLIFE = float(default=30.0)
    [[exq]]
        HOST = string(default=localhost)
        PORT = integer(default=9005)
        PROCESS = string(default=exq_daemon.py)
        PINGLIFE = float(default=30.0)
    [[power]]
        HOST = string(default=localhost)
        PORT = integer(default=9006)
        PROCESS = string(default=power_daemon.py)
        PINGLIFE = float(default=10.0)
    [[dome]]
        HOST = string(default=localhost)
        PORT = integer(default=9007)
        PROCESS = string(default=dome_daemon.py)
        PINGLIFE = float(default=10.0)
    [[ota]]
        HOST = string(default=localhost)
        PORT = integer(default=9008)
        PROCESS = string(default=ota_daemon.py)
        PINGLIFE = float(default=10.0)
    [[intf1]]
        HOST = string(default=localhost)
        PORT = integer(default=9020)
        PROCESS = string(default=ut_interface.py)
        PINGLIFE = float(default=10.0)
    [[intf2]]
        HOST = string(default=localhost)
        PORT = integer(default=9021)
        PROCESS = string(default=ut_interface.py)
        PINGLIFE = float(default=10.0)
    [[conditions]]
        HOST = string(default=localhost)
        PORT = integer(default=9030)
        PROCESS = string(default=conditions_daemon.py)
        PINGLIFE = float(default=60.0)
    [[scheduler]]
        HOST = string(default=localhost)
        PORT = integer(default=9031)
        PROCESS = string(default=scheduler_daemon.py)
        PINGLIFE = float(default=60.0)
    [[sentinel]]
        HOST = string(default=localhost)
        PORT = integer(default=9032)
        PROCESS = string(default=sentinel_daemon.py)
        PINGLIFE = float(default=60.0)

[UTS]

[POWER_UNITS]
    [[PDU1]]
        CLASS = string(default='FakePDU')
        IP = string(default='0.0.0.0')
        NAMES = string_list(default=list('p1', 'p2', 'p3', 'p4', 'p5', 'p6', 'p7', 'p8'))
    [[UPS1]]
        CLASS = string(default='FakeUPS')
        IP = string(default='0.0.0.0')
        NAMES = string_list(default=list('u1', 'u2', 'u3'))

[POWER_GROUPS]

[AUTOFOCUS_PARAMS]
